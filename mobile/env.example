# Variables d'environnement pour les builds EAS
# Copiez ce fichier en .env et remplissez les valeurs réelles
# NE COMMITTEZ JAMAIS le fichier .env avec les vraies valeurs

# ============================================
# Android Keystores (base64 encodés)
# ============================================
# Générer avec: base64 credentials/keystore-{profil}.jks | tr -d '\n'
ANDROID_KEYSTORE_BASE64_DEV=
ANDROID_KEYSTORE_BASE64_PREVIEW=
ANDROID_KEYSTORE_BASE64_PROD=

# Mots de passe du keystore (générés automatiquement - voir guide pas à pas)
# Utilisez les valeurs générées dans credentials/passwords-*.txt
ANDROID_KEYSTORE_PASSWORD_DEV=
ANDROID_KEYSTORE_PASSWORD_PREVIEW=
ANDROID_KEYSTORE_PASSWORD_PROD=

# Alias de la clé (défini lors de la génération)
ANDROID_KEY_ALIAS_DEV=bolibana_sugu_dev
ANDROID_KEY_ALIAS_PREVIEW=bolibana_sugu_preview
ANDROID_KEY_ALIAS_PROD=bolibana_sugu_prod

# Mots de passe de la clé (souvent identique au storepass)
ANDROID_KEY_PASSWORD_DEV=
ANDROID_KEY_PASSWORD_PREVIEW=
ANDROID_KEY_PASSWORD_PROD=

# ============================================
# iOS Certificats et Provisioning (base64 encodés)
# ============================================
# Certificat de distribution (.p12 encodé en base64)
EXPO_APPLE_DIST_CERT_BASE64=

# Provisioning profile (.mobileprovision encodé en base64)
EXPO_APPLE_PROV_PROFILE_BASE64=

# Mot de passe du certificat .p12
EXPO_APPLE_DIST_CERT_PASSWORD=

# Apple ID pour les opérations automatisées
EXPO_APPLE_ID=

# Team ID Apple Developer
EXPO_APPLE_TEAM_ID=

# App-specific password (pour les uploads)
EXPO_APPLE_APP_SPECIFIC_PASSWORD=

# ============================================
# EAS Configuration
# ============================================
# Profil de build actif (development, preview, production)
EAS_BUILD_PROFILE=development

# ============================================
# Notes importantes
# ============================================
# 1. Les fichiers .jks, .p12, .mobileprovision ne doivent JAMAIS être commités
# 2. Stockez les valeurs base64 dans un gestionnaire de secrets (GitHub Secrets, 1Password, etc.)
# 3. Pour plus de détails, consultez mobile/docs/keystore-setup.md

